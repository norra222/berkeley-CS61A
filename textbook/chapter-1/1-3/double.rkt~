#lang racket

; Exercise 1.41

(define (double f)
  (lambda (x) (f (f x))))

(define (inc x) (+ x 1))

(define (add-2 x) (+ x 2))

(((double (double double)) inc) 5)

(((double (double double)) add-2) 0)